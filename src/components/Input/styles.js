import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: '#0070f3',
    padding: 10,
    borderRadius: 10,
    marginBottom: 10,
  },
});

export default styles;
